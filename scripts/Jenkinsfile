#!groovy

node {
    stage('Example') {
        try {
            sh 'exit 1'
        }
        catch (err) {
            echo 'Something failed, I should sound the klaxons!'
            throw err
        }
    }
}
